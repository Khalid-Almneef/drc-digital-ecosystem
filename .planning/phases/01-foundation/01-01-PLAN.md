---
wave: 1
depends_on: []
files_modified: ["package.json", "tailwind.config.ts", "src/app/globals.css", "src/app/layout.tsx", "src/components/ui/Navigation.tsx"]
autonomous: true
---

# Plan: 01-01 - Foundation & Brand System Setup

## Goal
Set up the frontend framework (Next.js + Tailwind CSS) and integrate the official visual identity (colors, fonts, logos) as per `01-UI-SPEC.md` and `01-CONTEXT.md` to prepare the blank canvas for the main website implementation.

## Requirements
- FND-05
- FND-03

## Execution Steps

<task>
1. Initialize Project Structure
<action>
Initialize a new Next.js project using App Router, TypeScript, and Tailwind CSS in the workspace root if it doesn't already exist.
Ensure the `package.json` includes `framer-motion` for animations, and `lucide-react` for icons.
</action>
<read_first>
- `.planning/phases/01-foundation/01-CONTEXT.md`
</read_first>
<acceptance_criteria>
- `package.json` contains "next", "tailwindcss", and "framer-motion".
- The `src/app` directory exists.
</acceptance_criteria>
</task>

<task>
2. Inject Brand Design Tokens
<action>
Update `tailwind.config.ts` to include the specific theme variables derived from the brand identity files. Setup CSS variables in `src/app/globals.css` if necessary for colors and typography settings based on the UI-SPEC constraints.
</action>
<read_first>
- `.planning/phases/01-foundation/01-UI-SPEC.md`
- `tailwind.config.ts`
- `src/app/globals.css`
</read_first>
<acceptance_criteria>
- `tailwind.config.ts` contains custom `colors` and `extend` properties corresponding to brand assets.
</acceptance_criteria>
</task>

<task>
3. Create Premium Navigation Component
<action>
Develop a `Navigation.tsx` component in `src/components/ui/` featuring a glassmorphism sticky header. Include the club's logo placeholder and links for 'Overview', 'Hackathons', and 'Alumni', plus a 'Join Us' button. Include initial Framer Motion entrance variations for testing.
</action>
<read_first>
- `.planning/phases/01-foundation/01-UI-SPEC.md`
</read_first>
<acceptance_criteria>
- `src/components/ui/Navigation.tsx` exists and imports `framer-motion`.
</acceptance_criteria>
</task>

<task>
4. Integrate Blank Canvas Layout
<action>
Implement the Navigation component into the global `src/app/layout.tsx`. Remove the default Next.js boilerplate from `src/app/page.tsx` leaving a clean, blank main container ready for the subsequent phases' sections.
</action>
<read_first>
- `src/app/layout.tsx`
- `src/app/page.tsx`
</read_first>
<acceptance_criteria>
- `src/app/layout.tsx` renders `<Navigation />`.
- `src/app/page.tsx` is cleared of Next.js startup code and returns a minimal wrapper.
</acceptance_criteria>
</task>

## Verification

### Must Haves
- [ ] Next.js + Tailwind + Framer Motion dependencies installed.
- [ ] Tailwind configuration extended with the proper UI-SPEC branding constraints context.
- [ ] Navigation header created with glassmorphism logic.

### Verification Criteria
- `npm run lint` and TypeScript compilation pass without errors.
- Visual inspection checking that `globals.css` properly applies typography definitions.
